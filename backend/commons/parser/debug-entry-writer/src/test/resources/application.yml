kraken:
  gatling:
    results:
      root: gatling/results
  container:
    name: name
    host-id: hostId
    task-id: taskId
    task-type: GATLING_DEBUG

logging:
  level:
    root: INFO
  com:
    kraken: INFO

spring:
  error:
    include-stacktrace: always
  main:
    web-environment: false