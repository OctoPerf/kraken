import { SplitPaneDirective } from './split-pane.directive';

describe('SplitPaneDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitPaneDirective();
    expect(directive).toBeTruthy();
  });
});
