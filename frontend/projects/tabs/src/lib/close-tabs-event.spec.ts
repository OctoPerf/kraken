import {CloseTabsEvent} from 'projects/tabs/src/lib/close-tabs-event';

describe('CloseTabsEvent', () => {

  it('should create an instance', () => {
    expect(new CloseTabsEvent()).toBeTruthy();
  });
});
